import { CreatorDashboard } from "@ui/patterns/dashboard/creator-dashboard";

export const metadata = { title: "Dashboard — Learnify" };

export default function Page() {
  return <CreatorDashboard />;
}
