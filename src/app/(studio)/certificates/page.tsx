import { CertificatesScreen } from "@ui/patterns/certificates/certificates-screen";

export const metadata = { title: "Certificates — Learnify" };

export default function Page() {
  return <CertificatesScreen />;
}
