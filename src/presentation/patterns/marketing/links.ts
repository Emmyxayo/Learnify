/**
 * A real published course, used wherever the marketing pages want to
 * show the product rather than describe it.
 *
 * A live sales page proves more than a directory of them, and this is
 * the only public course surface that exists — there is no marketplace
 * index yet. When one lands, the secondary CTA can point there instead
 * and this constant is the only thing that changes.
 */
export const EXAMPLE_COURSE_PATH = "/c/graceleadership/digital-marketing-masterclass";
